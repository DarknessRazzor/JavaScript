/**
 * Ejercicio 4
 * Escribir una función a la que le pasemos un argumento y nos indique si este es un objeto Date o no.
 * */

function isDate(arg){
    if (arg === Date){
        console.log("El argumento tiene formato Date")
    } else {
        console.log("El argumento NO tiene formato Date")
    }

}
isDate(array);